# 类数组转数组

1. Array.prototype.slice.call()

```javascript
target = Array.prototype.slice.call(source)
```

2. Array.from()

```javascript
target = Array.from(source)
```

3. 扩展运算符

```javascript
target = [...source]
```
